name: AOSMDex Bot

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  bot:
    runs-on: ubuntu-latest

    steps:
      - name: Baixar código
        uses: actions/checkout@v4

      - name: Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Instalar dependências
        run: npm install

<<<<<<< HEAD
      - name: Registrar comandos
        env:
          DISCORD_TOKEN: ${{ secrets.DISCORD_TOKEN }}
        run: node deploy-commands.js

=======
      - name: Configurar comandos
        env:
          DISCORD_TOKEN: ${{ secrets.DISCORD_TOKEN }}
        run: node deploy-commands.js
        
>>>>>>> 1f9a2c7 (Corrige token do bot)
      - name: Iniciar bot
        env:
          DISCORD_TOKEN: ${{ secrets.DISCORD_TOKEN }}
        run: node index.js
